# GreytHR Attendance Automation Configuration
# Copy this file to .env and update with your actual values

# ===== GREYTHR CREDENTIALS =====
# Your company's GreytHR URL (e.g., https://company.greythr.com)
GREYTHR_URL=https://your-company.greythr.com

# Your GreytHR login credentials
GREYTHR_USERNAME=your_username_or_email
GREYTHR_PASSWORD=your_password

# ===== SCHEDULE CONFIGURATION =====
# Time to automatically sign in (24-hour format, IST timezone)
SIGNIN_TIME=09:00

# Time to automatically sign out (24-hour format, IST timezone)
SIGNOUT_TIME=19:00

# ===== RETRY CONFIGURATION =====
# Retry strategy: "exponential" (default) or "fixed"
# - exponential: 5min → 15min → 45min → 135min → 405min
# - fixed: same delay for all retry attempts
RETRY_STRATEGY=exponential

# Maximum number of retry attempts per action (sign-in/sign-out)
MAX_RETRY_ATTEMPTS=5

# For exponential backoff strategy: Base delay in seconds (default: 300 = 5 minutes)
# Each retry multiplies by 3: 5min → 15min → 45min → 135min → 405min
BASE_RETRY_DELAY=300

# For fixed delay strategy: Fixed delay in seconds (default: 300 = 5 minutes)
# All retries use the same delay: 5min → 5min → 5min → 5min → 5min
FIXED_RETRY_DELAY=300

# ===== TESTING & DEBUGGING =====
# Enable test mode to run on weekends and bypass normal restrictions
# Set to "true" for testing, "false" for production
TEST_MODE=false

# ===== EXAMPLE CONFIGURATIONS =====

# Example 1: Conservative exponential backoff (longer delays)
# RETRY_STRATEGY=exponential
# BASE_RETRY_DELAY=600
# MAX_RETRY_ATTEMPTS=3

# Example 2: Aggressive fixed retry (shorter, frequent retries)
# RETRY_STRATEGY=fixed
# FIXED_RETRY_DELAY=180
# MAX_RETRY_ATTEMPTS=10

# Example 3: Production-safe settings
# RETRY_STRATEGY=exponential
# BASE_RETRY_DELAY=300
# MAX_RETRY_ATTEMPTS=5

# Example 4: Testing configuration
# TEST_MODE=true
# RETRY_STRATEGY=fixed
# FIXED_RETRY_DELAY=60
# MAX_RETRY_ATTEMPTS=3
